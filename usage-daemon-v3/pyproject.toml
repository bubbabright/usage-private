[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "usage-daemon-v3"
version = "0.1.0"
description = "Python rewrite of usage-daemon: poll LLM/API provider quotas and expose them over HTTP (zero JS)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["httpx>=0.27"]

[dependency-groups]
dev = ["pytest>=8"]

[project.scripts]
usage-daemon = "usage_daemon.__main__:main"
usage-daemon-v3 = "usage_daemon.__main__:main"
usage = "usage_daemon.cli:main"

[tool.setuptools.packages.find]
include = ["usage_daemon*"]

[tool.pytest.ini_options]
testpaths = ["tests"]